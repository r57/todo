type Query {
  helloWorld: String!
}

type ListItemItems {
  content: String,
  done: Boolean!,
  created: String!,
  assigned: String,
}

type ListItem {
  id: String,
  title: String,
  description: String,
  items: [ListItemItems], 
}